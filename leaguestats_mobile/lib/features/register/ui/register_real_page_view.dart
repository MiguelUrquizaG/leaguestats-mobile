import 'package:flutter/material.dart';

class RegisterRealPageView extends StatelessWidget {
  const RegisterRealPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Registro Real')),
      body: Center(
        child: Text('Aquí va el formulario real de registro'),
      ),
    );
  }
}
